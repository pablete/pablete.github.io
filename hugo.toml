baseURL = "https://pablodelgado.org/"
languageCode = "en-us"
title = "Share what you know"
enableRobotsTXT = true

[params]
  subtitle = "Learn what you don't"
  author = "Pablo Delgado"
  description = "Pablo Delgado's blog"
  showPosts = false             # flip to true when ready to surface /posts/ in the nav

[permalinks]
  talks = "/blog/:year/:month/:day/:slug/"
  posts = "/posts/:year/:month/:day/:slug/"

[markup]
  [markup.goldmark.renderer]
    unsafe = true
  [markup.highlight]
    noClasses = false
    lineNos = false
    tabWidth = 2

[outputs]
  home = ["HTML", "RSS"]
